export async function askOpenAI(p:string){const k=process.env.OPENAI_API_KEY;if(!k)throw Error("OPENAI_API_KEY not configured");const r=await fetch("https://api.openai.com/v1/chat/completions",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${k}`},body:JSON.stringify({model:process.env.OPENAI_MODEL||"gpt-4o-mini",messages:[{role:"user",content:p}]})});if(!r.ok)throw Error(`OpenAI ${r.status}`);const j=await r.json();return j.choices?.[0]?.message?.content||"No answer"} export async function askGemini(p:string){const k=process.env.GEMINI_API_KEY;if(!k)throw Error("GEMINI_API_KEY not configured");const m=process.env.GEMINI_MODEL||"gemini-2.0-flash";const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${m}:generateContent?key=${k}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({contents:[{parts:[{text:p}]}]})});if(!r.ok)throw Error(`Gemini ${r.status}`);const j=await r.json();return j.candidates?.[0]?.content?.parts?.[0]?.text||"No answer"} export const demo=(p:string)=>`Demo answer for "${p}". Add provider API keys in Vercel to enable real AI.`;